# Cloud Run buildpack dependencies for the AI Newsroom webapp (`make deploy-webapp`).
# This file is the ONE sanctioned requirements.txt for the webapp — the Cloud Run
# `--source` buildpack needs it. Keep the google-adk pin in sync with pyproject.toml.
flask>=3.1.0
gunicorn>=23.0.0
google-cloud-aiplatform[agent-engines]>=1.130.0
google-genai>=1.69.0
google-adk>=2.0.0
google-auth>=2.0.0
python-dotenv>=1.0.0
